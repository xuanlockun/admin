{
  "name": "cms-admin-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute cms_posts --local --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute cms_posts --remote --file=./schema.sql",
    "db:migrate": "wrangler d1 migrations apply cms_posts --local",
    "db:migrate:remote": "wrangler d1 migrations apply cms_posts --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
